# Trivy vulnerability suppressions
# Format: CVE-ID  # comment
# Review all entries before expiry dates and remove once patched.

# CVE-2026-45829: Pre-auth RCE in ChromaDB Python FastAPI server (CRITICAL).
# CVE-2026-45830: Cross-tenant authorization bypass (HIGH).
# CVE-2026-45831: SimpleRBACAuthorizationProvider scope bug (HIGH).
# CVE-2026-45833: Authenticated trust_remote_code code injection (HIGH).
# No patch available as of 2026-08-24. Affects chromadb 1.0.0–1.5.9.
# Mitigation: ChromaDB port is NOT published to the host in docker-compose.yml
# (internal Docker network only, unreachable from outside the compose stack).
# Remove these entries and upgrade chromadb once a patched version is released.
# Expiry: 2027-02-24
CVE-2026-45829
CVE-2026-45830
CVE-2026-45831
CVE-2026-45833
